syntax = "proto3";
package service;

// Test ping service.
service PingService {
    // method
    rpc Ping (Message) returns (Message) {
    }
}

// Test pong service.
service PongService {
    // method
    rpc Pong (Message) returns (Message) {
    }
}

message Message {
    string msg = 1;
}

message Class {
    int64 value = 1;
}